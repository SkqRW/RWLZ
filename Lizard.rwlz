[BepInPlugin("RainLizard", "Lizard Mod", "1.0")]

Move(int speed, float power) {
    int steps = 10;
    print("moviendo al jugador");
    if (steps > 5) {
        return <prop>(speed);
    } else {
        return 0;
    }
}

