[BepInPlugin("RainLizard", "Lizard Mod", "1.0")]

/* ------------------- |
|   Archivo de prueba  |
|                     */

bool Move(int speed, float power) {
    int steps = 10;
    print("moviendo al jugador");
    if (steps > 5) {
        // modificar el speed
        return false;
    } else {
        return true;
    }
}

