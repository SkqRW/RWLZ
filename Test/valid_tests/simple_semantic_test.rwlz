// Simple semantic test
int add(int a, int b) {
    return a + b;
}

int main() {
    int x = 10;
    int y = 20;
    int result = add(x, y);
    
    const int MAX = 100;
    
    if (result > 0) {
        x = x + 1;
    }
    
    return 0;
}
